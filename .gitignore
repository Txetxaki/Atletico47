
# Datos de produccion: viven solo en la Pi y se respaldan con scripts/respaldo-db.sh
# Dependencias
# Endpoints personales de cada dispositivo suscrito.
# Índice local de CodeGraph
# Secretos: la clave de la API del Coach y la clave VAPID privada.
coach.json
.codegraph/
datos/
*.db
*.db-shm
*.db-wal
.env*
.env*.local
node_modules/
package-lock.json
respaldos/
servidor-push/agenda.json
servidor-push/config.json
servidor-push/suscripciones.json
.vercel
